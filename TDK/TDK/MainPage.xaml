<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:TDK.MapsCustoms"
             x:Class="TDK.MainPage"
             BackgroundColor="{StaticResource standardBackground}">
    <Grid>
        <local:CustomMap x:Name="locationsMap" 
              HorizontalOptions="FillAndExpand" 
              VerticalOptions="FillAndExpand"
              MapType="Hybrid"
              IsShowingUser="True"
              />
        <Button x:Name="statsButton"
            HorizontalOptions="StartAndExpand"
            VerticalOptions="End"
            Text="Odkryte Miejsca"
            Clicked="StatsButton_Clicked"
            BackgroundColor="{StaticResource greenFlagKepno}"
            TextColor="{StaticResource whiteFlagKepno}"
            Margin="25"
            CornerRadius="0"/>
    </Grid>
    

</ContentPage>
